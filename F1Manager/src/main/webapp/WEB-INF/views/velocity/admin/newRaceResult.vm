<html>
	<head>
		<title></title>
	</head>
<body>
uj futam eredmeny form<br>
#if($races.size() != 0)
    <form action="saveRaceResult" method="post">
		
		race<select name="race.id">
		#foreach($race in $races)
				<option value="$race.id">$race.date</option>
		#end
		</select>
		
		<p>#if($!sameDriver)Ugyanaz a pilota ketszer szerepelt egy oszlopban#end</p>
			
		helyezes - im - verseny<br>
		#foreach($d in $drivers)
			$velocityCount
				
				<select name="qualifyingDrivers">
        		#foreach($driver in $drivers)
        				<option value="$driver.id">$driver.name</option>
        		#end
				</select>
				
				<select name="raceDrivers">
        		#foreach($driver in $drivers)
        				<option value="$driver.id">$driver.name</option>
        		#end
				</select>
				
				<br>	
		#end

    	<input type="submit" value="save"></input>
    </form>	
#else
	<p>Minden futamhoz van allitva eredmeny</p>
#end
</body>
</html>
